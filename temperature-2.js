// Fahrenheit temperature
const fahrenheitTemperature = 45;

// Conversion formula: (f-32) × 5/9
const celsiusTemperature = ((fahrenheitTemperature - 32) * 5) / 9;

// Output the result
console.log(
  `${fahrenheitTemperature} degrees fahrenheit is equal to ${celsiusTemperature} degrees celsius.`
);
